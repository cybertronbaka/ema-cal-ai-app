import 'package:flutter/material.dart';

class SetGenderView extends StatelessWidget {
  const SetGenderView({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
